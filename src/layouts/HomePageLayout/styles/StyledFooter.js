import styled from 'styled-components';

const StyledFooter = styled.div`
  height: 80px;
  background: #ffffff;
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: "chalkboard SE";
  color: #acacac;
  box-shadow: inset 0 1px 4px 0 rgba(0,0,0,0.21);
  font-size: 1.5em;
`;

export default StyledFooter;